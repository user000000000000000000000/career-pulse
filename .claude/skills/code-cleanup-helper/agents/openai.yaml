interface:
  display_name: "Code Cleanup Helper"
  short_description: "Run reproducible code, skill, and repository audits"
  default_prompt: "Use $code-cleanup-helper to run a read-only audit and report evidence before changing anything."
